{
  "name": "@ew/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Economy Warfare web client — Vite + React SPA. Embeddable as an iframe modal.",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "prepare-art": "node scripts/prepare-art.mjs",
    "prebuild": "node scripts/prepare-art.mjs"
  },
  "dependencies": {
    "@ew/engine": "*",
    "@ew/shared": "*",
    "framer-motion": "^11.5.4",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/node": "^22.5.0",
    "@types/react": "^18.3.5",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.1",
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
